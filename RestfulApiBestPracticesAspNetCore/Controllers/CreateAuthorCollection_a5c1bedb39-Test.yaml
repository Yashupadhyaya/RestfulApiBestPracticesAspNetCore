# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "CreateAuthorCollection_a5c1bedb39-Test.csv"
    fields:
      - "name"
      - "age"
      - "books"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            name: "{{ name }}"
            age: "{{ age }}"
            books: "{{ books }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "Id"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid"
          json:
            name: "{{ name }}"
            age: "{{ age }}"
            books: "{{ books }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            name: "{{ name }}"
            age: "{{ age }}"
            books: "{{ books }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ statusCode }}"
            - hasProperty: "Id"
