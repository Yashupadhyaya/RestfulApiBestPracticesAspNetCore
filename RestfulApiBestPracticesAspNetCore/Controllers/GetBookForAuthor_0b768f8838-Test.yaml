# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetBookForAuthor_0b768f8838-Test.csv"
    fields: ["authorId", "id"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - post: 
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "bookForAuthor"
  - name: "API request with validation failure"
    flow:
      - post: 
          url: "/invalidURL"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404
  - name: "API request with edge case"
    flow:
      - post: 
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "bookForAuthor"
            - hasHeader: "Content-Type"
