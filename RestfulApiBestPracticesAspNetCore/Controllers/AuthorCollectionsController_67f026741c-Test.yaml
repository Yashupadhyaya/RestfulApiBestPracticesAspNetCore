# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "AuthorCollectionsController_67f026741c-Test.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "data"
            - hasHeader: "content-type"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid"
          json:
            field1: "{{ field1 }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasHeader: "content-type"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            field1: "{{ field1 }}"
            field2: "largeDataOrSpecialCharacters"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "data"
            - hasHeader: "content-type"
