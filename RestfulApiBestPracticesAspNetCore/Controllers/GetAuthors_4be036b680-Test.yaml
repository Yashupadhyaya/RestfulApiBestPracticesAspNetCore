# Test generated by RoostGPT for test test_csapi using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetAuthors_4be036b680-Test.csv"
    fields:
      - "authorId"
      - "authorName"
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/{{ authorId }}"
          headers:
            Accept: "application/vnd.marvin.hateoas+json"
          expect:
            - statusCode: 200
            - hasProperty: "links"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidUrl"
          headers:
            Accept: "application/vnd.marvin.hateoas+json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ authorId }}"
          headers:
            Accept: "application/vnd.marvin.hateoas+json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "links"

  - name: "API request to create author"
    flow:
      - post:
          url: "/create"
          json:
            authorName: "Test Author"
          headers:
            Content-Type: "application/vnd.marvin.author.full+json"
          expect:
            - statusCode: 201
            - hasProperty: "links"

  - name: "API request to delete author"
    flow:
      - delete:
          url: "/{{ authorId }}"
          headers:
            Accept: "application/vnd.marvin.hateoas+json"
          expect:
            - statusCode: 204
