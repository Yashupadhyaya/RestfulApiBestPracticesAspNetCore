# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "7314b9e8c1-Test.csv"
    fields: ["id", "patchDtos"]
    skipHeader: true
scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            id: "{{ id }}"
            patchDtos: "{{ patchDtos }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "patchDtos"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid-url"
          json:
            id: "{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            id: "{{ id }}"
            patchDtos: "{{ patchDtos }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "id"
            - hasProperty: "patchDtos"
