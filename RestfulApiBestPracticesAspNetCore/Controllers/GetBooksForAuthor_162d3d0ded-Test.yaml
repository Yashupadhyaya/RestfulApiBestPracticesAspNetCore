# Test generated by RoostGPT for test test_csapi using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetBooksForAuthor_162d3d0ded-Test.csv"
    fields:
      - "authorId"
      - "bookId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/{{ authorId }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "booksForAuthor"
            - hasHeader: "Content-Type"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid-url"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ authorId }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "booksForAuthor"
            - hasHeader: "Content-Type"

  - name: "CreateBookForAuthor"
    flow:
      - post:
          url: "/{{ authorId }}"
          json:
            title: "New Book"
            description: "New book description"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 201
            - hasProperty: "bookToReturn"
            - hasHeader: "Content-Type"

  - name: "DeleteBookForAuthor"
    flow:
      - delete:
          url: "/{{ authorId }}/{{ bookId }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 204

  - name: "UpdateBookForAuthor"
    flow:
      - put:
          url: "/{{ authorId }}/{{ bookId }}"
          json:
            title: "Updated Book"
            description: "Updated book description"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 204

  - name: "PartiallyUpdateBookForAuthor"
    flow:
      - patch:
          url: "/{{ authorId }}/{{ bookId }}"
          json:
            title: "Partially Updated Book"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 204
