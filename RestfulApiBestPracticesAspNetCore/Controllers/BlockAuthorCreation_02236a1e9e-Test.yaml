# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "BlockAuthorCreation_02236a1e9e-Test.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            id: "{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 409
            - hasProperty: "headers"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid_url"
          json:
            id: "{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            id: "{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: function(res) { return res.statusCode >= 200 && res.statusCode < 300; }
            - hasProperty: "headers"
