# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "DeleteBookForAuthor_f4ad09eea4-Test.csv"
    fields:
      - "authorId"
      - "id"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 204
            - hasHeader: "content-type"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 204
            - hasHeader: "content-type"
            - body: "{{ 'Book ' + id + ' for author ' + authorId + ' was deleted.' }}"
