# Test generated by RoostGPT for test cs-apitest using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/DeleteBookForAuthor"
          json:
            authorId: "valid-author-id"
            id: "valid-book-id"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 204

  - name: "API request with validation failure - non existing author"
    flow:
      - post:
          url: "/DeleteBookForAuthor"
          json:
            authorId: "non-existing-author-id"
            id: "valid-book-id"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with validation failure - non existing book"
    flow:
      - post:
          url: "/DeleteBookForAuthor"
          json:
            authorId: "valid-author-id"
            id: "non-existing-book-id"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with edge case - large data"
    flow:
      - post:
          url: "/DeleteBookForAuthor"
          json:
            authorId: "large-data-author-id"
            id: "large-data-book-id"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ 200..500 }}"
