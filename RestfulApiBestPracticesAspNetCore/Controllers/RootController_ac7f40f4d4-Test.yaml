# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./RootController_ac7f40f4d4-Test.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          headers:
            content-type: "application/json"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 200
            - hasProperty: "data"
            - hasHeader: "content-type"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "invalid-url"
          headers:
            content-type: "application/json"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasHeader: "content-type"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          headers:
            content-type: "application/json"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "data"
            - hasHeader: "content-type"
