# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "DeleteAuthor_050ed2de56-Test.csv"
    fields: ["id"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            id: "{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 204
            - hasProperty: "headers"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid"
          json:
            id: "{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            id: "{{ largeData }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "headers"
