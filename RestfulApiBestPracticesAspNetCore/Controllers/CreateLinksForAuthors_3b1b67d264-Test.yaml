# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "CreateLinksForAuthors_3b1b67d264-Test.csv"
    fields:
      - "authorsResourceParameters"
      - "hasNext"
      - "hasPrevious"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            authorsResourceParameters: "{{ authorsResourceParameters }}"
            hasNext: "{{ hasNext }}"
            hasPrevious: "{{ hasPrevious }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "links"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid"
          json:
            authorsResourceParameters: "{{ authorsResourceParameters }}"
            hasNext: "{{ hasNext }}"
            hasPrevious: "{{ hasPrevious }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            authorsResourceParameters: "{{ authorsResourceParameters }}"
            hasNext: "{{ hasNext }}"
            hasPrevious: "{{ hasPrevious }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "links"
