# Test generated by RoostGPT for test cs-apitest using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/CreateLinksForAuthors"
          json:
            authorsResourceParameters: "dummyParameters"
            hasNext: true
            hasPrevious: false
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "links"
            - equals:
                "links[0].rel": "self"
                "links[0].method": "GET"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/CreateLinksForAuthors"
          json:
            authorsResourceParameters: ""
            hasNext: true
            hasPrevious: false
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "message"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/CreateLinksForAuthors"
          json:
            authorsResourceParameters: "dummyParametersWithSpecialCharacters"
            hasNext: true
            hasPrevious: false
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: function(response) { return response.statusCode >= 200 && response.statusCode < 300; }
            - matchJSON: ".links[0]", /self/
