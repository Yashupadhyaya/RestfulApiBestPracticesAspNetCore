# Test generated by RoostGPT for test cs-apitest using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.com"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/books"
          json:
            authorId: "123e4567-e89b-12d3-a456-426614174000"
            title: "Artillery Test Book"
            description: "This is a test book for artillery"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "Id"
            - hasProperty: "title"
            - hasProperty: "description"
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/books"
          json:
            authorId: "123e4567-e89b-12d3-a456-426614174000"
            title: "Artillery Test Book"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/books"
          json:
            authorId: "123e4567-e89b-12d3-a456-426614174000"
            title: "Artillery Test Book with Special Characters !@#$%^&*()_+"
            description: "This is a test book for artillery with large data{{ $loopCount }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "Id"
            - hasProperty: "title"
            - hasProperty: "description"
            - equals: ["description", "This is a test book for artillery with large data{{ $loopCount }}"]
