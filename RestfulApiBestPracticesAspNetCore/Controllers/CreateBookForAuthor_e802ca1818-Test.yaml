# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "CreateBookForAuthor_e802ca1818-Test.csv"
    fields:
      - "authorId"
      - "bookDto"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            bookDto: "{{ bookDto }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "bookToReturn"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            bookDto: "{{ bookDto }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "message"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            bookDto: "{{ bookDto }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(400, 500) }}"
            - hasProperty: "message"

  - name: "API request with Invalid URL"
    flow:
      - post:
          url: "/invalid"
          json:
            authorId: "{{ authorId }}"
            bookDto: "{{ bookDto }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404
