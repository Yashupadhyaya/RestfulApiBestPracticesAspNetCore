# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "PartiallyUpdateBookForAuthor_cd8eb3dc97-Test.csv"
    fields:
      - "authorId"
      - "id"
      - "patchDoc"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
            patchDoc: "{{ patchDoc }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "bookToReturn"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
            patchDoc: "{{ patchDoc }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
            patchDoc: "{{ patchDoc }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ statusCode }}"
            - hasProperty: "response"
