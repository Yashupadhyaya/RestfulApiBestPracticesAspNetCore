# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "CreateAuthorWithDateOfDeath_7a281a80cf-Test.csv"
    fields: ["author"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            author: "{{ author }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 201
            - hasProperty: "Id"
            - hasProperty: "links"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid-url"
          json:
            author: "{{ author }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            author: "{{ author }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "Id"
            - hasProperty: "links"
