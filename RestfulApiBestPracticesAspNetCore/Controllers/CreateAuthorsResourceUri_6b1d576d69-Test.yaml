# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "CreateAuthorsResourceUri_6b1d576d69-Test.csv"
    fields:
      - "fields"
      - "orderBy"
      - "searchQuery"
      - "genre"
      - "pageNumber"
      - "pageSize"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post: "/"
        headers:
          Content-Type: "application/json"
        json:
          fields: "{{ fields }}"
          orderBy: "{{ orderBy }}"
          searchQuery: "{{ searchQuery }}"
          genre: "{{ genre }}"
          pageNumber: "{{ pageNumber }}"
          pageSize: "{{ pageSize }}"
        expect:
          - statusCode: 200
          - hasProperty: "data"

  - name: "API request with validation failure"
    flow:
      - post: "/invalid-url"
        headers:
          Content-Type: "application/json"
        json:
          fields: "{{ fields }}"
          orderBy: "{{ orderBy }}"
          searchQuery: "{{ searchQuery }}"
          genre: "{{ genre }}"
          pageNumber: "{{ pageNumber }}"
          pageSize: "{{ pageSize }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - post: "/"
        headers:
          Content-Type: "application/json"
        json:
          fields: "{{ fields }}"
          orderBy: "{{ orderBy }}"
          searchQuery: "{{ searchQuery }}"
          genre: "{{ genre }}"
          pageNumber: "{{ pageNumber }}"
          pageSize: "{{ pageSize }}"
        expect:
          - statusCode: 200
          - hasProperty: "data"
