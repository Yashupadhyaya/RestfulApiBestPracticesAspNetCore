# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  payload:
    path: "CreateLinksForBook_0d1d0b6752-Test.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post: "/"
        headers:
          Content-Type: "application/json"
        json:
          id: "{{ id }}"
        expect:
          - statusCode: 200
          - hasProperty: "Links"

  - name: "API request with validation failure"
    flow:
      - post: "/invalid-url"
        headers:
          Content-Type: "application/json"
        json:
          id: "{{ id }}"
        expect:
          - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post: "/"
        headers:
          Content-Type: "application/json"
        json:
          id: "{{ id }}"
        expect:
          - statusCode: "{{ $between(200, 500) }}"
          - hasProperty: "Links"
