# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "UpdateBookForAuthor_01a123c049-Test.csv"
    fields:
      - "authorId"
      - "id"
      - "bookForUpdateDto"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
            bookForUpdateDto: "{{ bookForUpdateDto }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "Content-Type"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
            bookForUpdateDto: null
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "Content-Type"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
            id: "{{ id }}"
            bookForUpdateDto: "{{ bookForUpdateDto }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "Content-Type"
