# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "BooksController_0fbd57da27-Test.csv"
    fields:
      - "bookId"
      - "title"
      - "author"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            bookId: "{{ bookId }}"
            title: "{{ title }}"
            author: "{{ author }}"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "bookId"
            - hasProperty: "title"
            - hasProperty: "author"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/"
          json:
            bookId: "{{ bookId }}"
            title: "{{ title }}"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "message"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            bookId: "{{ bookId }}"
            title: "{{ title }}"
            author: "{{ author }}"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "bookId"
            - hasProperty: "title"
            - hasProperty: "author"
