# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "CreateLinksForAuthor_d276dea82f-Test.csv"
    fields:
      - "id"
      - "fields"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            id: "{{ id }}"
            fields: "{{ fields }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "links"
            - hasHeader: "Content-Type"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid"
          json:
            id: "{{ id }}"
            fields: "{{ fields }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            id: "{{ id }}"
            fields: "{{ fields }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "links"
            - hasHeader: "Content-Type"
