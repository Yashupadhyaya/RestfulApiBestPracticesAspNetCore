# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "AuthorsController_9f32cf9534-Test.csv"
    fields:
      - "authorId"
      - "authorName"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          headers:
            Content-Type: "application/json"
          json:
            authorId: "{{ authorId }}"
            authorName: "{{ authorName }}"
          expect:
            - statusCode: 200
            - hasProperty: "authorId"
            - hasProperty: "authorName"
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid-url"
          headers:
            Content-Type: "application/json"
          json:
            authorId: "{{ authorId }}"
          expect:
            - statusCode: 400
  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          headers:
            Content-Type: "application/json"
          json:
            authorId: "{{ authorId }}"
            authorName: "{{ authorName }}"
          expect:
            - statusCode:
                min: 200
                max: 500
            - hasProperty: "authorId"
            - hasProperty: "authorName"
