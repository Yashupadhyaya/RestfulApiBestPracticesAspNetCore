# Test generated by RoostGPT for test test_csapi using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetRoot_2dfbaa6cbf-Test.csv"
    fields:
      ["mediaType"]
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/"
          headers:
            Accept: "{{ mediaType }}"
          expect:
            - statusCode: 200
            - hasProperty: "links"
            - equals:
                - "links[0].rel": "self"
                - "links[1].rel": "authors"
                - "links[2].rel": "create_author"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid-url"
          headers:
            Accept: "{{ mediaType }}"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - get:
          url: "/"
          headers:
            Accept: "invalid/mediaType"
          expect:
            - statusCode: 204
