# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "CreateAuthor_046c58eb1b-Test.csv"
    fields: ["author"]
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            author: "{{ author }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "Id"
            - hasProperty: "links"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid"
          json:
            author: "{{ author }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            author: "{{ author }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "Id"
            - hasProperty: "links"
