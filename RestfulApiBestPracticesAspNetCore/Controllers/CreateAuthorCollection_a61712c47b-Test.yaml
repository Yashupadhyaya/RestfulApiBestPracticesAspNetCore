# Test generated by RoostGPT for test test_csapi using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "CreateAuthorCollection_a61712c47b-Test.csv"
    fields:
      - "authorName"
      - "authorSurname"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/authorcollections"
          json:
            authorName: "{{ authorName }}"
            authorSurname: "{{ authorSurname }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "authorName"
            - hasProperty: "authorSurname"
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/authorcollections"
          json:
            authorName: "{{ authorName }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/authorcollections"
          json:
            authorName: "{{ authorName }}"
            authorSurname: "{{ authorSurname }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "authorName"
            - hasProperty: "authorSurname"
