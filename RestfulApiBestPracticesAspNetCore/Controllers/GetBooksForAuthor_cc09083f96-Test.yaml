# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetBooksForAuthor_cc09083f96-Test.csv"
    fields:
      - "authorId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            authorId: "{{ authorId }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "booksForAuthor"
            - hasHeader: "Content-Type"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalidurl"
          json:
            authorId: "{{ authorId }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            authorId: "special_characters_or_large_data"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "booksForAuthor"
            - hasHeader: "Content-Type"
