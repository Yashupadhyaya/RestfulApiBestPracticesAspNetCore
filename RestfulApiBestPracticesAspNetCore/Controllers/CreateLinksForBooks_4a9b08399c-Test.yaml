# Test generated by RoostGPT for test demo567 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "CreateLinksForBooks_4a9b08399c-Test.csv"
    fields:
      - "bookId"
      - "authorId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post: 
          url: "/"
          json:
            bookId: "{{ bookId }}"
            authorId: "{{ authorId }}"
          expect:
            - statusCode: 200
            - hasProperty: "Links"
            - hasProperty: "Links.self"

  - name: "API request with validation failure"
    flow:
      - post: 
          url: "/invalidUrl"
          json:
            bookId: "{{ bookId }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post: 
          url: "/"
          json:
            bookId: "{{ bookId }}"
            authorId: "{{ authorId }}"
          headers:
            Content-Type: "application/json"
            Accept: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "Links"
            - hasProperty: "Links.self"
            - hasHeader: "Content-Type"
