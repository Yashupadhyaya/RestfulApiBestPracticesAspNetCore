# Test generated by RoostGPT for test cs-apitest using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:2500/api"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/authors"
          expect:
            - statusCode: 200
            - hasProperty: "authors"
            
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/authors/{id}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            
  - name: "API request with edge case"
    flow:
      - get:
          url: "/authors/{id}"
          expect:
            - statusCode: 200
            - hasProperty: "authors"
            - equals:
                authors[0].name: "John Doe"

  - name: "POST request to create blog story"
    flow:
      - post:
          url: "/api/blog-story/create-blog-story"
          json:
            Name: "something555"
            CreatedDate: "2022-01-01T00:00:00Z"
            DatePublished: "2022-01-01T00:00:00Z"
            Title: "somethingvery"
            Slug: "ha-something"
          expect:
            - statusCode: 201
            - hasProperty: "createdBlogStory"
            - equals:
                createdBlogStory.Name: "something555"
