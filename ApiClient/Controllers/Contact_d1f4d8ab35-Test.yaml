# Test generated by RoostGPT for test csharpapitest using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/contact"
          expect:
            - statusCode: 200
            - hasProperty: "Message"
            - equals: 
                - "Message": "Your contact page."

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/contact"
          headers:
            dummyHeader: "invalidHeader"
          expect:
            - statusCode: 400
            - hasProperty: "Error"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/contact"
          headers:
            dummyHeader: "specialCharacters@#"
          expect:
            - statusCode: 200
            - hasProperty: "Message"
            - equals: 
                - "Message": "Your contact page."
